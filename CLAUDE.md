# CLAUDE.md — rocket-sales-simulator

## 프로젝트 개요

로켓런칭 프로그램 웨비나 매출 시뮬레이션 웹 서비스.
기존 스프레드시트 기반 시뮬레이션을 직관적인 웹 UI로 구현.

## 핵심 목표

- 웨비나 매출 시뮬레이션을 쉽고 직관적으로 사용할 수 있는 웹 서비스
- 파트너사 또는 내부 용도로 활용 가능

## 기술 스택

- 단일 HTML 파일 (index.html) — 외부 의존성 없음
- localStorage 기반 시나리오 저장
- 배포: GitHub Pages (fancy-rgb.github.io/rocket-sales-simulator)

## 작업 체크리스트

- [x] 기획 및 요구사항 정의
- [x] 기술 스택 결정
- [x] UI/UX 설계
- [x] 개발 (index.html — 4탭 구성)
- [x] GitHub 레포 생성 및 연결
- [x] GitHub Pages 배포
- [x] v1.2.0 보안 업그레이드 (Firebase Auth + 접근 제어 + 소프트 삭제)
- [x] v2.0.0 Supabase 이전 완료 (Auth + Postgres, 2026-05-13)

## 다음 단계

### 카테고리별 벤치마크 기능

업종(카테고리)을 선택하면 해당 업종의 과거 데이터 기반 기준값(CAC, 참석률, FT참석률, 결제CVR 등)을 자동으로 제안하는 기능.

**구현 계획 (옵션 3)**
1. 웨비나 RAW 시트에 카테고리 컬럼 추가 (사용자 작업)
2. Google Sheets MCP로 데이터 읽어 카테고리별 평균 산출 (Claude 작업)
3. 시뮬레이터에 초기 벤치마크 데이터 하드코딩 + 직접 수정 가능한 UI 추가

**선행 작업**: 웨비나 RAW 시트에 컬럼 2개 추가 필요
- `카테고리` (드롭다운 — 아래 7개 코드로 고정)
- `가격대` (드롭다운 — 고가/중가/입문)

---

## 카테고리 분류 체계 (확정 — 2026-04-09)

### 1축: 분야 (7개 고정)

| 코드 | 분야 | 포함 예시 |
|------|------|----------|
| MKT | 마케팅/광고 | SNS마케팅, 퍼포먼스, 브랜딩 |
| BIZ | 창업/비즈니스 | 스마트스토어, 부업, 프리랜서 |
| FIN | 재테크/투자 | 부동산, 주식, 경매 |
| BEAUTY | 뷰티/라이프 | 네일, 피부, 패션 |
| HEALTH | 건강/다이어트 | 헬스, 필라테스, 식단 |
| EDU | 취업/커리어 | 이직, 자격증, 멘토링 |
| OTHER | 기타 | 분류 어려운 경우 (데이터 쌓이면 재분류) |

### 2축: 가격대 (3구간)

| 구간 | 범위 |
|------|------|
| 고가 | 100만원 이상 |
| 중가 | 30~100만원 |
| 입문 | 30만원 미만 |

### 분류 판단 기준 (헷갈릴 때)

- 주 타겟 구매 동기가 "수익/돈" → FIN 또는 BIZ
- 주 타겟 구매 동기가 "직업/취업" → EDU
- 주 타겟 구매 동기가 "외모/건강" → BEAUTY 또는 HEALTH
- 애매하면 OTHER로 임시 분류 후 데이터 쌓이면 재분류

> 드롭다운으로 입력 제한 필수 — 자유 텍스트 허용 시 MCP 읽기 오염 발생

---

## 업종 어휘 정합 (2026-07-15 — benchmarks 정본)

시뮬레이터 업종 드롭다운·`benchmarks.industry`는 운영 콘솔 `webinars.industry` 실제 값을 그대로 쓴다.
아래가 정본이며, 위의 7개 코드(MKT/BIZ 등)는 구 설계(레거시)다.

| 실제 업종 값(정본) | 표본 n | 벤치마크 | 구 코드(참고) |
|---|---|---|---|
| 창업·부업 | 24 | 실측 | BIZ |
| 뷰티·라이프스타일 | 23 | 실측 | BEAUTY |
| 부동산·투자 | 15 | 실측 | FIN |
| 교육비즈니스 | 13 | 실측 | EDU |
| 건강·의료 | 8 | 표본부족→전체 pooled | HEALTH |
| 마케팅·브랜딩 | 2 | 표본부족→전체 pooled | MKT |
| 디자인·크리에이티브 | 2 | 표본부족→전체 pooled | OTHER |
| 취업·커리어 | 1 | 표본부족→전체 pooled | EDU |

표본 n≥10만 업종별 실측, 나머지는 전체 pooled 대체(코드에서 is_fallback=true).

---

## 보안 운영 가이드

### 접근 제어 설계 (확정 — 2026-04-15)

이 서비스는 **내부 전용**이다. 외부 접근을 허용할 이유가 없으므로 구글 로그인(Firebase Authentication)으로 접근을 제한한다.

**허용 도메인**
- `@liveklass.com`
- `@futureschole.com`

**차단 목록 관리 방식**
- 앱 내 관리 UI 없음 — Firebase 콘솔에서 직접 관리
- 이유: 퇴사자 처리는 연 1~3회 수준이므로 별도 UI를 만드는 것은 유지 비용이 더 크다

---

### 퇴사자 접근 차단 절차

팀원이 퇴사하면 아래 절차를 따른다.

1. [console.firebase.google.com](https://console.firebase.google.com) 접속
2. 프로젝트 `rocket-simulator-f988e` 선택
3. 좌측 메뉴 → **Firestore Database**
4. `blocked_users` 컬렉션 → **문서 추가**
5. 필드 입력:
   - 필드명: `email`
   - 값: 퇴사자 이메일 주소 (예: `hong@liveklass.com`)
6. 저장 → 즉시 적용 (해당 계정은 다음 접속 시 자동 차단)

> Firebase 콘솔 접근 권한이 없으면 개발자(태윤)에게 요청한다.

---

### 관리자 추가/제거 절차

1. Firebase 콘솔 → `rocket-simulator-f988e` → **Firestore Database**
2. `admins` 컬렉션 선택
3. **추가**: `+ 문서 추가` → 문서 ID = 이메일 주소, 필드 `email` = 이메일 주소
4. **제거**: 해당 문서 선택 → 삭제

즉시 적용 (다음 로그인 시 반영).

---

## 릴리즈 작업 규칙

### 시작 전 체크리스트 (작업 전에 만들고 하나씩 체크)

```
[ ] 구현할 기능/수정 목록 확정
[ ] 영향받는 기존 코드 참조 grep으로 사전 확인
[ ] 보안 변경 포함 시 → 코드 의존성 먼저 수정 계획 수립
```

### 버전 기록 절차 (배포 전 반드시 확인) — 단일 소스 + CI 감시

> 배경: 과거 이 체크리스트는 "3곳을 손으로 함께 수정"이었으나, Firebase→Supabase 전환(v2.0.0)에서 3곳 모두 누락됐다(체크리스트는 있었으나 트리거되지 않아 조용히 건너뜀). 그래서 ①헤더 배지는 손으로 고칠 곳 자체를 없애고(단일 상수) ②나머지 일치는 CI가 감시하도록 구조화했다.

**헤더 배지는 더 이상 손으로 고치지 않는다.** `index.html`/`index-v2.html` 최상단 스크립트의 `const APP_VERSION` 값만 바꾸면 배지가 자동으로 그 값을 표시한다.

**릴리즈(기능 배포) 시 함께 올려야 하는 곳:**

```
live 버전(index.html):
[ ] const APP_VERSION 값
[ ] 앱 내 changelog 모달 최상단 <h3>vX.Y.Z — 날짜</h3> 항목 추가
[ ] CHANGELOG.md 최상단 ## [vX.Y.Z] — 날짜 항목 추가
    → 이 3개의 버전 문자열이 서로 같아야 한다.

preview 버전(index-v2.html, 승격 전까지):
[ ] const APP_VERSION 값 (예: v2.1.0-preview)
[ ] 앱 내 모달 최상단 <h3> 항목
    → 이 2개가 서로 같아야 한다. (CHANGELOG.md는 live 정본이므로 preview와 별개)
```

**CI 감시** — `.github/workflows/version-sync.yml`이 push마다 위 일치를 검사한다. 어긋나면 커밋에 ❌(빨간불)가 뜬다. (GitHub Pages가 브랜치 직접 배포라 배포를 "차단"하진 않고 "경고"한다. 하드 차단이 필요하면 Pages를 Actions 배포로 전환.)

**로컬 사전 확인**(선택):
```bash
grep -m1 -oE 'const APP_VERSION *= *"[^"]+"' index.html   # 배지 버전
grep -m1 '<h3>v' index.html                                # 모달 최상단
grep -m1 '^## \[v' CHANGELOG.md                            # CHANGELOG 최상단
```

---

## 배포 후 검증 체크리스트

기능 배포 후 반드시 아래 항목을 순서대로 확인한다.

### 인증
- [ ] 허용 도메인 계정(@liveklass.com / @futureschole.com)으로 정상 로그인
- [ ] 외부 계정(gmail 등)으로 로그인 시 🚫 접근 불가 화면 표시
- [ ] 로그아웃 → 로그인 오버레이 재표시

### 시나리오 저장
- [ ] 시뮬레이션 탭에서 저장 → 작성자가 로그인 계정 이름으로 자동 지정
- [ ] 시나리오 탭에서 저장된 카드 확인

### 삭제 권한
- [ ] 관리자 계정 로그인 → 삭제 버튼 + 카드 체크박스 표시
- [ ] 일반 계정 로그인 → 삭제 버튼 없음 확인

### 소프트 삭제
- [ ] 시나리오 삭제 후 Firebase 콘솔에서 해당 문서에 `deleted: true`, `deletedBy`, `deletedAt` 필드 확인
- [ ] 삭제된 시나리오가 목록에서 제외되는지 확인

### 로딩
- [ ] 시나리오 탭 진입 시 스피너 표시 후 목록으로 전환 확인
